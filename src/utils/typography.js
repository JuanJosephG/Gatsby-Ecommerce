import Typography from "typography"

const typography = new Typography({
  baseFontSize: "26px",
  baseLineHeight: 100,
  headerFontFamily: ["Lato", "Helvetica Neue", "Arial"],
  bodyFontFamily: ["Dosis", "Roboto", "Georgia"],
})

export default typography
